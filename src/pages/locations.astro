---
import MainLayout from '@layouts/MainLayout.astro';
import LocationTile from '@components/ui/LocationTile';
import { locationData } from '@data/locationData';

export interface Props {
  locationData?: {
    id: string;
    name: string;
    address: {
      name: string;
      street: string;
      city: string;
    };
    contact: {
      name: string;
      phone: string;
      email: string;
    };
    geolocation: {
      lat: number;
      lng: number;
    };
  };
  index: number;
}
---

<MainLayout>
  <section class='flex flex-col gap-10 md:gap-28 lg:gap-8'>
    {
      locationData.map((location, index) => (
        <LocationTile
          client:only='react'
          locationData={location}
          index={index}
        />
      ))
    }
  </section>
</MainLayout>
